fn main() {
    println!("use 'rustc <file name>' to build rust file.");
    println!("use 'cargo new <project name>' to create new rust project with cargo.");
    println!("use 'cargo check' to to validate the rust project with cargo won't crash at build.");
    println!("use 'cargo run' to build and run rust project with cargo.");
    println!("use 'cargo build' to build project with cargo.");
    println!("use 'cargo build --release' to build the production executable project with cargo.");
}
